let score;
let date;
let name;

export function createVerifier(token = null, score, date, name) {
    score = score;
    date = date;
    name = name;
    return verifier
}

export function verify(token) {

}